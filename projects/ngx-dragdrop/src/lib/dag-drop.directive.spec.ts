import { DagDropDirective } from './dag-drop.directive';

describe('DagDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DagDropDirective();
    expect(directive).toBeTruthy();
  });
});
