import { DagDragDirective } from './dag-drag.directive';

describe('DagDragDirective', () => {
  it('should create an instance', () => {
    const directive = new DagDragDirective();
    expect(directive).toBeTruthy();
  });
});
